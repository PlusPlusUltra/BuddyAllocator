int toFree [];
int writeHere;
int currentNode;
while (true)
{
	clear currentNode
	if(leftSon(currentNode) < max){
		 toFree[writeHere] = leftSon(currentNode);
		 writHere++;
		 toFree[writeHere] = rightSOn(currentNode);
		 writeHere++;
	 }
	 
}
